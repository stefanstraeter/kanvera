/**
 * @description Theme color constants synchronized with styles/core/variables.css
 * Light mode: --color-ghost-white (#eaeff8) / --bg-app-body in light mode
 * Dark mode:  --color-very-dark-grey (#20212c) / --bg-app-body in dark mode
 * Used for meta[name="theme-color"] to control browser chrome color on iOS Safari.
 */
(function () {
    window.THEME_COLORS = {
        light: "#eaeff8",
        dark: "#20212c"
    };
})();
