// src/utils/input-validation.js

/**
 * Prüft, ob ein Textfeld Inhalt hat
 */
export function validateNotEmpty(value) {
    // Hier kommt später die Logik rein
    return false;
}

/**
 * Prüft das E-Mail Format
 */
export function validateEmailFormat(email) {
    // Hier kommt später die Logik rein
    return false;
}

/**
 * Steuert die Anzeige der Fehlermeldungen im HTML
 */
export function toggleErrorMessage(inputElement, isValid, message) {
    // Hier greifen wir später auf .error-msg zu
}